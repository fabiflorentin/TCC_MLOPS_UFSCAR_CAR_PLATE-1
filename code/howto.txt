Copy best.pt to the same dir as app.py

docker build -t detect .

docker run -d -e AWS_ACCESS_KEY_ID= -e AWS_SECRET_ACCESS_KEY= -e AWS_DEFAULT_REGION=us-east-1 -p 8000:8000 --name detect detect

GET http://54.164.186.26:8000
   "API is working !!"

POST http://localhost:8000/detectPlate
{
 "bucket_name": "upload-img-tes-stage",
 "object_key": "img_010002.jpg"
}
